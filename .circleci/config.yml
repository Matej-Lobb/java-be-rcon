version: 2.1

jobs:
  build:
    docker:
      - image: circleci/openjdk:14-jdk-buster
    steps:
      - checkout
      - run: mvn clean compile
      - run: mvn install jacoco:report
      - store_artifacts:
          path: target/be-rcon-1.2.0-SNAPSHOT.jar
      - run:
          name: Analyze on SonarCloud
          command: mvn verify sonar:sonar
